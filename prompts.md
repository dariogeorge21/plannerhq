I want you to design my product UI UX. Currently as a senior frontend developer.

# Dashboard

I want an enhanced user dashboard which would with mock data and events for better visual understanding of the application.

The user dashboard does not have a sidebar. Instead on the top use breadcrumbs for navigation.
The first section would be the KPI's, it would include:
1. Total workspaces (out of /as per plan)
2. Joined Workspaces
3. Current Plan

## Worspaces List

Shows the section of workspaces, list them, if null then tells to create one. 

-> Contains a create modal, update modal, archieve modal. 
-> View: List wise. Each with serial number, name, last accessed, edit, delete option 
-> Sorted based on last accessed. (DESCENDING)


## Next Section: Application Settings: 

    -> Profile Settings: name, email (non editable), hqid(random id), theme of the website(default bright),
    -> Notification Preferences: Push Notifications, Email Notifications, In-app Notifications


## Current Plan and Subscriptions

-> Subscription and plan management.